import React from 'react';
// import intl from 'react-intl-universal';
import Page from '@/components/Page';

const Security = () => {
  return <Page title="安全设置"></Page>;
  // return <Page title={intl.get('sms.sensitive.title')}></Page>;
};

export default Security;
